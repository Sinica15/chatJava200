package Server.Users;

import Server.Server;
import org.junit.Test;
import org.mockito.Mock;
import org.mockito.Mockito;

import java.net.ServerSocket;
import java.net.Socket;
import java.util.HashMap;

import static org.junit.Assert.*;
import static org.mockito.ArgumentMatchers.any;
import static org.mockito.ArgumentMatchers.anyString;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;

public class UserSTest {

    @Test
    public void connectionWith() {

//        Socket socket = mock(Socket.class);
//        HashMap<Integer, User> clientArrMock = new HashMap<Integer, User>();

//        UserS user1 = new UserS(1, "Socket", socket);
//        UserS user2 = new UserS(1, "Socket", socket);

//        UserS user3 = mock(UserS.class);

//        when(user3.lookingFor(any()))



//        user1.connectionWith(user2);

//        assertEquals(user1.Interlocutor, user2);
//        assertEquals(user2.Interlocutor, user1);


    }

    @Test
    public void setStatus() {
    }
}